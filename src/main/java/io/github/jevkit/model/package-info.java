/**
 * Questions you ask and the answers you get back: {@link io.github.jevkit.model.ChoiceQuestion},
 * {@link io.github.jevkit.model.ScoreQuestion}, {@link io.github.jevkit.model.NoulQuestion} and their answers.
 *
 * <p>Everything here is immutable and validated on construction, so an invalid question fails where it is built rather
 * than when it is sent.
 */
package io.github.jevkit.model;
